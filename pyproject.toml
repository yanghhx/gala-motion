[project]
name = "gala-motion"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["torch>=2.4", "numpy>=1.24", "pyyaml>=6.0", "tensorboard>=2.14"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

